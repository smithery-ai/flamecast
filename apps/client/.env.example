# VITE_API_URL=https://flamecast-backend.vercel.app/api
VITE_API_URL=http://localhost:8787/api

# Set to "true" to show the developer sidebar with runtimes.
# When false (default), the app auto-starts the default runtime and shows the new tab page.
# VITE_DEVELOPER_PREVIEW=true