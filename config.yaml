# Flamecast API server — which state manager backs connection metadata + logs.
# - psql: Postgres (FLAMECAST_POSTGRES_URL) or local PGLite under .acp/pglite if unset
# - memory: in-process only; data is lost when the server exits
stateManager: psql
