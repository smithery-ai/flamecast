{
  "name": "@flamecast/example-cloudflare",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "pnpm -w turbo run build:package --filter=@flamecast/example-cloudflare... && tsc --project tsconfig.json --noEmit",
    "deploy": "wrangler deploy",
    "test": "vitest run --config vitest.config.ts"
  },
  "dependencies": {
    "@agentclientprotocol/sdk": "^0.16.1",
    "@flamecast/runtime-e2b": "workspace:*",
    "@flamecast/sdk": "workspace:*",
    "@flamecast/storage-psql": "workspace:*",
    "better-auth": "^1.2.0",
    "dotenv": "^17.3.1",
    "drizzle-orm": "^0.45.1",
    "hono": "^4.12.8",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "wrangler": "^4.77.0"
  },
  "packageManager": "pnpm@10.26.2"
}
