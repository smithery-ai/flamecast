/**
 * Re-exports from @flamecast/protocol.
 * Kept for backward compatibility with existing import paths.
 */
export type {
  FileSystemEntry,
  PermissionRequestEvent,
  FilesystemSnapshotEvent,
  FilePreviewEvent,
  PermissionRespondAction,
  FsSnapshotAction,
  FilePreviewAction,
  SessionHostStartRequest,
  SessionHostStartResponse,
  SessionHostHealthResponse,
} from "@flamecast/protocol/session-host";
